require "spec_helper"

describe "not getting too much for free", type: :request do

  it "should require us to specify new behaviour" do
    fail "see CryptoCurrenciesController#create_bitcoin"
  end

end
